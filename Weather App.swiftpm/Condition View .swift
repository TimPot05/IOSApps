import SwiftUI

struct Conditionview: View{
    
    let image: Image
    let color: Color
    
    var body: some View{
        // Box View with letter
        ZStack{
            
            Image(systemName: "sun.max.fill")                .foregroundColor(.yellow)
        }
    }
}
